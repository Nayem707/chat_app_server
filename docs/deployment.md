# Deployment

> Placeholder. Populated in **Phase 10** — production hardening.

Planned coverage:

- Container image build + registry push.
- MongoDB Atlas / self-hosted replica set considerations.
- Environment variable hygiene (secret rotation, `.env` handling).
- Reverse proxy config (nginx / Caddy) for HTTPS + WebSocket upgrade.
- Horizontal scaling (Socket.IO Redis adapter).
- Logging, metrics, health checks, graceful shutdown.
